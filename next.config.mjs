/** @type {import('next').NextConfig} */
const nextConfig = {
  eslint: {
    ignoreDuringBuilds: true,
  },
  typescript: {
    ignoreBuildErrors: true,
  },
  images: {
    unoptimized: true,
  },
  // PostHog reverse proxy: route analytics through our own domain (/ingest)
  // so ad/tracking blockers don't drop events. US cloud destinations.
  async rewrites() {
    return [
      {
        source: "/ingest/static/:path*",
        destination: "https://us-assets.i.posthog.com/static/:path*",
      },
      {
        source: "/ingest/:path*",
        destination: "https://us.i.posthog.com/:path*",
      },
    ];
  },
  // Required so the proxied /ingest/decide (no trailing slash) isn't redirected.
  skipTrailingSlashRedirect: true,
}

export default nextConfig
